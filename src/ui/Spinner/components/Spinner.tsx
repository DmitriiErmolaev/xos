import React from 'react';
import '../../../assets/ui/spinner.scss';

const Spinner = () => {
  return (
    <div className='spinner__container'>
      Spinner...
    </div>
  );
};

export default Spinner;